import { Loading } from "@/components/ui/progress"

export default function LoadingComp(){
    return (
        <div className="flex custom-gradient justify-center items-center h-screen">
        <Loading /> 
      </div>
    )
}